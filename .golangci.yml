version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - durationcheck
    - errname
    - errorlint
    - gocritic
    - gosec
    - misspell
    - nilerr
    - noctx
    - nolintlint
    - perfsprint
    - predeclared
    - revive
    - sloglint
    - unconvert
    - unparam
    - usetesting
    - wastedassign
    - whitespace

  settings:
    revive:
      rules:
        - name: exported
        - name: package-comments
        - name: var-naming
        - name: indent-error-flow
        - name: error-strings
        - name: context-as-argument
        - name: unreachable-code
    sloglint:
      # The project logs with key and value pairs, never with slog.Attr.
      kv-only: true
      static-msg: false

  exclusions:
    # The templ files carry a "Code generated" header.
    generated: lax
    rules:
      # The code under internal/components and internal/utils comes from the
      # shadcn-templ registry, and part of it is generated. An upgrade
      # overwrites it, so a fix there does not survive. The findings are
      # style and false positives, for example a long string in the icon data
      # that gosec reads as a password.
      - path: internal/(components|utils)/
        linters:
          - errcheck
          - gocritic
          - gosec
          - nilerr
          - revive
          - staticcheck
          - unparam
      # cmd/renderhtml is a tool that a developer runs by hand. The output
      # directory comes from the command line, which gosec reads as a path
      # from an attacker.
      - path: cmd/renderhtml/
        linters:
          - gosec

formatters:
  enable:
    - gofumpt
    - goimports
  exclusions:
    generated: lax
    # The registry owns the shape of its own files.
    paths:
      - internal/components/
      - internal/utils/
